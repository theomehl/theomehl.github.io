---
layout: layouts/base.njk
title: Demos
description: Ideas I bothered to make real.
---

<main id="main" class="container flow">
    <h1>{{title}}</h1>
    <p>{{description}}</p>

    <ul class="blog-list flow">
        {% for post in collections.demos | reverse %}
        <li class="flow">
            <h2><a href="{{post.url}}">{{post.data.title}}</a></h2>
            <p>{{post.data.description}}</p>

        </li>
        {% endfor %}
    </ul>
</main>